[package]
name = "sptorch-hal-ffi"
version = "0.1.0"
edition = "2021"

[dependencies]
sptorch-hal = { version = "0.1.0", path = "../hal" }
sptorch-core-tensor = { version = "0.1.0", path = "../core-tensor" }
libloading = "0.8"

[dev-dependencies]
sptorch-core-ops = { version = "0.1.0", path = "../core-ops" }
